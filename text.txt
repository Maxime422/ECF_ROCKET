#zf #PokemonZf .navCard {
	padding: var(--padding-top-bottom-large);
	display: flex;
	justify-content: space-between;
	gap: var(--gap-medium);
}

#zf #PokemonZf .navCard div {
	padding-top: var(--padding-medium);
	display: flex;
	gap: var(--gap-medium);
}

/* Statistiques and Team */
#zf #PokemonZf .infos {
	border-top: var(--color-gray) solid 1px;
	padding: var(--padding-top-bottom-medium);
	display: flex;
}

#PokemonZf>* {
	display: flex;
	gap: var(--gap-large);
	flex-direction: column;
	padding: var(--padding-top-bottom-large);

}

#PokemonZf .infos div {
	display: flex;
	gap: var(--gap-medium);
}

/* Zf box */
#PokemonZf .sectionsStatistiques .zones {
	margin: var(--margin-top-bottom-medium);
	padding: var(--padding-medium);
	background-color: var(--color-black-1);
	border-radius: var(--border-radius-small);
}

/* Zf stats */
#PokemonZf .sectionsStatistiques .statistiques div {
	display: grid;
	grid-template-columns: 1fr 1fr;
	gap: var(--gap-small);
	grid-template-rows: auto;
}

#PokemonZf .sectionsStatistiques .team div {
	display: flex;
	padding: var(--padding-top-bottom-medium);
	align-items: center;
	gap: var(--gap-medium);
}

#PokemonZf .sectionsStatistiques .statistiques .secondaryText {
	grid-column: 1/3;
}

/* Pokeball Circle */
.circle {
	background-color: var(--color-gray);
	padding: var(--padding-small);
	border-radius: 50%;
	width: auto;
	height: 2rem;
}

.circleActive {
	background-color: var(--color-red-1);
}

#PokemonZf .sectionsStatistiques .zones {
	margin: 0;
}













/************** Update Species Pokemon **************/
async function updateSpeciesPokemon(url) {
	try {
		// Fetch du pokemon
		const dataSpecies = await getData(url);
		if (dataSpecies !== undefined && dataSpecies !== null) {
			if (document.querySelector('#evolutionBtn')) {
				await pokemonEvolutions(dataSpecies.evolution_chain.url);
			}

			const name = document.querySelector('#PokemonName');
			name.textContent = getName(dataSpecies);

			const description = document.querySelector('#description');
			description.textContent = getDescription(dataSpecies);

			const gen = document.querySelector('#generation');
			gen.textContent = getGeneration(dataSpecies);

			const id = document.querySelector('#IdPokemon');
			id.textContent = getId(dataSpecies);
		} else {
			console.error('Erreur lors dans les données species pokemon');
		}
	} catch (error) {
		console.error(error.message);
	}
}




			icon1.setAttribute('alt', `Type ${type1.textContent}`);
			icon1.setAttribute('loading', `lazy`);

            				icon2.setAttribute('alt', `Type ${type2.textContent}`);

                            			const talent = document.querySelector('#talent');